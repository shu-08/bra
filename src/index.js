import React from 'react';
import ReactDOM from 'react-dom'; // React 17では 'react-dom' を使用
import './index.css';
import App from './App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
